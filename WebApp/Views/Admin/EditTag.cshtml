@model WebApp.Entity.Tag

<!-- Container -->
<div class="container-fixed" id="content_container">
</div>
<!-- End of Container -->
<!-- Container -->
<div class="container-fixed">
    <div class="flex flex-wrap items-center lg:items-end justify-between gap-5 pb-7.5">
        <div class="flex flex-col justify-center gap-2">
            <h1 class="text-xl font-medium leading-none text-gray-900">
                Настройки сайта
            </h1>
            <div class="flex items-center gap-2 text-sm font-normal text-gray-700">
                Редактирование Тега
            </div>
        </div>
    </div>
</div>
<!-- End of Container -->
<!-- Container -->
<div class="container-fixed">
    <form class="card edit-tag-form" asp-controller="Admin" asp-action="EditTag" method="post" enctype="multipart/form-data">
        
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger mb-4">
                    @ViewBag.Error
                </div>
            }
        
        <div class="card-header">
            <h3 class="card-title">
               
            </h3>
        </div>
        <div class="card-body grid gap-5">

            <div class="flex items-baseline gap-2.5">
                <label class="form-label max-w-32">Id</label>
                <input class="input" name="Id" type="text" value="@Model.Id" readonly />
            </div>

            <div class="flex items-baseline gap-2.5">
                <label class="form-label max-w-32">Name</label>
                <input class="input" name="Name" type="text" value="@Model.Name" />
            </div>

            <div class="flex items-baseline gap-2.5">
                <label class="form-label max-w-32">Slug</label>
                <input class="input" name="Slug" type="text" value="@Model.Slug" />
            </div>

        </div>

        <div class="card-footer justify-center">
            <button class="btn btn-sm btn-primary" type="submit">
                Сохранить изменения
            </button>
        </div>
    </form>
    
</div>